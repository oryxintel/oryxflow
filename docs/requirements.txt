# Documentation build dependencies (Read the Docs / local `make html`).
# The oryxflow package itself is installed separately (see .readthedocs.yaml
# python.install -> method: pip, path: .), which brings in its runtime deps
# for sphinx.ext.autodoc.
sphinx>=9
sphinx_rtd_theme>=3
myst-parser>=5
